#include <stdio.h>
#include "main.h"

/**
 * int _strlen - a function that returns the length of a string
 *@s: string lenth declared
 * Return: Always 0
 */

int _strlen(char *s)
{

	length = strlen(s);

	return (0);
}

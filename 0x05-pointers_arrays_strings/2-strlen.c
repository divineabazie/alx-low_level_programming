#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 *_strlen - a function that returns the length of a string
 *@s: string lenth declared
 * Return: Always 0
 */

int _strlen(char *s)
{

	int length = _strlen(s);

	printf("%d\n", length);

	return (0);
}

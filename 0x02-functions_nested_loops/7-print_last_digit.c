#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - reutn value of last digit
 *
 * @x: x
 * Return: int
 */

int print_last_digit(int x)
{
	int y;

	x = (y % 10);

	if (x < 0)
	{
	x = (-1 * x);
	}

	_putchar('x' + 0);

return (x);


}

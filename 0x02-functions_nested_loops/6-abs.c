#include <stdio.h>
#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 *
 * @x: x
 * Return: int
 */

int _abs(int x)
{
	int y;
	int z; 

	if (x < 0)
		y = 0, z =0;

	else if (x > 0)
		y = 1, z = 1;
	else 
		y = -1, z = '-';


}

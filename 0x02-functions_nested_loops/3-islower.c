#include <stdio.h>
#include "main.h"

/**
 * _islower - a function that checks for lowercase characters
 *
 */

_islower(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
	{
	if ("c is lowercase")
		_putchar(c);
	}
	return (0);
	
	{
	else if ("c is otherwise")

		_putchar(c); 
	}
		return (1);
	

}
